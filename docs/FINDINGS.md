# Legaia 한글화 — 기술 문서 (FINDINGS)

Legend of Legaia (PS1) 한글 패치. **미국판(USA) 베이스.**
**게임 화면 한글 출력 성공** — ASM 훅 없이 (2026-07 검증).

> 이 문서 하나로 작업 재개 가능하도록 확정 사실만 기록.

---

## 0. 파일

| 이름 | 크기 | 비고 |
|------|------|------|
| Legend of Legaia (USA).bin | 466,714,416 | Mode2/2352, **베이스** |
| Legend of Legaia CN.bin | 466,714,416 | 중국어 팬패치 (레퍼런스) |
| Legaia Densetsu (Japan).bin | 467,443,536 | 일본판 (참고) |

섹터: `[0:12]sync [12:16]hdr [16:24]subhdr [24:2072]USER(2048) [2072:2352]EDC/ECC`

---

## 1. 디스크 구조 (US)

| 파일 | LBA | 크기 |
|------|-----|------|
| SCUS_942.54 (EXE) | 24 | 442,368 |
| PROT.DAT | 242 | 121,253,888 |

**EXE**: load `0x80010000`, entry `0x80026C28`, size `0x6B800`
→ `RAM = 0x80010000 + (파일오프셋 - 0x800)`

**CN판**: load `0x800AA000` (새 로더 + 압축블록 0x8800~0x38800, 엔트로피 7.75)

---

## 2. PROT.DAT → PACK → LZSS → Script

```
PROT: u32[0]=0, u32[1]=파일수(1235), u32[2..]=섹터오프셋 (offset<<11)
PACK: u32 fileCount, u32 totalDecLength, per entry: u24 decLength, u8 fileId, u32 compAddress
Script: 0x22 u16 count0/1/2, 0x28 u24 footer, 0x2B.. u24 포인터테이블, base=0x2B+count*3
```
맵 파일 **fileId 3 = 텍스트/스크립트**

**LZSS**: 링버퍼 4096, 시작 4078(0xFEE), 플래그 LSB-first,
bit=1 리터럴 / bit=0 u16LE `len=((v>>8)&0xF)+3, pos=((v&0xF000)>>4)|(v&0xFF)`,
**종료조건 = 해제후 크기**

---

## 3. 텍스트 (US = 평문 ASCII)

```
<1F> 텍스트런 시작 ... <00> 종료
예: <1F>The Memory Statue will<00><1F>remember things for you...<00>
```

| 코드 | 의미 |
|------|------|
| `0x00` | 문자열 종료 |
| `0x7C` | 제어 (1바이트) |
| `0xCE <n>` | 테이블 조회 삽입 (`0x80074050 + n*4`) |
| `0xCF <n>` | **팔레트 전환** → CLUT = n + 0x7F86 |
| 그 외 | **전부 글리프로 렌더** |

**매크로**: `0xC1`=플레이어이름(2482회), `0xC2`=아이템명(457), `0xAC`(503), `0xC7`(454)

---

## 4. 🔑 대사 렌더러 = `0x80036888`

**찾은 방법**: PCSX-Redux에서 텍스트버퍼 `0x800740EC` 에 Read 브레이크포인트.

```
텍스트 버퍼 = 0x800740EC   (하드코딩)

문자 c 렌더:
    U = (c & 0x0F) * 16
    V = (c & 0xF0) - 0x20
    글리프 14 x 15 px, GP0 0x64 스프라이트

CLUT = [0x8007B454](u16) + 0x7F86     ← 팔레트가 "변수"
    0xCF <n>  →  0x8007B454 = n

폭 = [0x80073F1C + c]                  ← 가변폭 테이블
    EXE 파일오프셋 0x6471C, 디스크 LBA 224
X += 폭 + 1
```

**배제된 후보**: `0x8001A374`(8x8 HUD폰트), `0x8001A078`(printf류),
`0x8001ABC8`/`0x8001C7A0`(숫자), `0x8001BE80`(3D정점)

---

## 5. 폰트

- PROT.DAT 파일0 내 TIM: 헤더 `0x7F40`, **픽셀 `0x7F80`**, 256×256 4bpp
- VRAM (896,0), tpage 0x0E
- 타일에디터로 열 때: 파일0-상대 오프셋 `0x6780`
- ASCII 배치: 16열 격자, U/V는 위 공식대로

### 니블 값 (핵심)
- **US 원본은 값 {0,1,2,14,15} 만 사용** → 잉크 = **14, 15**
- **224개 셀 전부에서 값 1/2/3 이 완전히 비어 있음** (검증됨)
- CN 패치는 16값 전부 사용 (듀얼레이어)

### 팔레트
- TIM `0x11218` 의 CLUT = 16색 × **16팔레트**, VRAM (0,510)
- **PROT 오프셋 `0x1122C`**
- CLUT id = `arg + 0x7F86`, 팔레트 row = `arg + 6`
- 대사가 쓰는 arg: 0(기본), 4,5,6,7,9 (색 강조)
- **arg 1,2,3,8 (row 7,8,9,14) 미사용** → 한글용으로 재정의 가능

---

## 6. ✅ (A)방식 한글 출력 — 성공

**ASM 훅 불필요.** 폰트 + 팔레트 + 텍스트 + 폭테이블만 수정.

1. **한글을 니블 값 1/2 로** 폰트에 그림 (ASCII는 14/15 → 충돌 없음)
2. **팔레트 row 7** (`0xCF 01`) 을 한글 전용으로:
   값1=흰색, 값2=회색, **값14/15=투명**
3. **기존 사용 팔레트**(row 6,10,11,12,13,15)의 값1/2/3 → **투명화**
   (안 하면 한글이 영어 대사에도 나타남)
4. 텍스트: `0xCF 01` + 한글코드 + `0xCF 00`
   **길이를 원문과 동일하게** → 포인터 재계산 불필요
5. **폭 테이블 패치 필수**: 한글 코드 폭 → 13px
   ⚠️ 안 하면 글자가 겹침 (v1 실패 원인)

빌드: `tools/build_us_kr2.py` → `tools/write_us.py`

---

## 6-B. ✅ 완성형 음절 PoC — 성공 (품질 우수)

`tools/build_us_kr3.py`. (A)방식 팔레트 트릭 그대로, 글리프만 **완성형 음절**로.

- 음절을 Unifont 16px → 13×14 로 축소해 14×15 셀에 렌더 (값 1/2)
- 게임 화면 확인: "기억의 상은 / 당신을 기억합니다" 선명하게 출력됨
- **조합형(자모)보다 압도적으로 품질 좋음** → 완성형 채택 확정

### 조합형은 폐기
`tools/jamo.py`, `jamo2.py` 에 1벌식/2벌식 구현했으나:
- 12~15px 해상도에서 자모를 재조립하면 획이 뭉개짐
- 2벌식도 81자로 가용코드에 빠듯
- **완성형이 모든 면에서 우위** → 조합형 폐기 (코드는 참고용으로 보존)

---

## 7. ⚠️ 코드 공간 (현재 병목) — 1바이트의 벽

**하드 리밋**: 코드 `c` → `V = (c & 0xF0) - 0x20`
- 상위니블 0x0, 0x1 → V 음수 → **사용 불가**
- 유효 상위니블 = 0x2~0xF (14행) × 16열 = **224칸이 절대 한계**

**그중 실제 안전한 코드**:
- 영어가 ASCII(0x21~0x7E) 대부분 사용
- 0x80+ 는 매크로/제어로 119종 사용 (0xC1=이름 3517회, 0xC2=아이템 611회 등)
- **전체 맵 기준 완전 안전 = 4개** (`91 DB E3 F5`)
- **단일 맵(map6) 기준 = 72개** ← PoC는 이걸로 성공

**실전 번역 규모**: 영어 대사 25,272개 런 / 약 495,743자
→ 한글 고유 음절 **700~1000자** 필요 → **1바이트로는 절대 불가**

---

## 8. 🔜 2바이트 훅 설계 (다음 작업)

### 훅 지점
```
0x80036960: bne $v1, $v0, 0x80036B24    ; v1=문자, v0=0xCE
                                        ; 0xCE 아니면 -> 글리프 경로(0x80036B24)
```
여기를 훅으로 리다이렉트. 훅에서:
1. 문자가 확장마커(예 `0x92~0x95`)인지 검사
2. 맞으면 다음 바이트 읽어 음절 인덱스 = `(마커-0x92)*256 + next`
3. U/V 계산 → 음절 폰트 페이지 좌표
4. **tpage 전환 프리미티브 삽입** (음절 폰트가 다른 VRAM 페이지)
5. 스프라이트 프리미티브 생성 (14×15)
6. `s3 += 2`, X 전진
7. 마커 아니면 원래 경로(0x80036B24)로 복귀

### 프리미티브 구조 (20바이트, GP0 0x64)
```
+0x00 tag        0x04000000
+0x04 code+color 0x64808080
+0x08 X (s16)
+0x0A Y (s16)
+0x0C U (u8)      ← 8비트! 페이지 내 0~255
+0x0D V (u8)      ← 8비트
+0x0E CLUT (u16)
+0x10 W = 14
+0x12 H = 15
```
**tpage 필드 없음** → GP0 0xE1 (DR_TPAGE) 프리미티브를 별도 삽입해야 함.
- (896,0) 4bpp → tpage `0x000E`
- (896,256) 4bpp → tpage `0x001E`

### ✅ 훅 코드 배치 = `0x8007AC00` (에뮬레이터 실측으로 확정)
- EXE 오프셋 `0x6B400`, 디스크 LBA 238
- 833바이트 연속 빈 공간 = MIPS 명령 208개
- EXE text 영역(0x80010000~0x8007B800) 안 → 자동 로드, 별도 로더 불필요
- **PCSX-Redux Write 브레이크포인트로 게임진행중 쓰기 0회 확인**

배제된 후보 (실측):
- `0x80080000`: 게임이 씀 (Write 12회) ❌
- `0x800AA000`: 전투 시 씀 (Write 1회) ❌ — CN로더가 여기 있지만 부팅후 게임이 덮어씀
- `0x800797D0`/`0x8007AB38`/`0x80079004`: BSS, 코드가 참조 ❌

### tpage 처리 (핵심)
- 렌더러는 tpage를 프리미티브에 안 넣고 GPU 전역상태에 의존
  (폰트가 항상 (896,0) 페이지라 재설정 불필요했음)
- 프리미티브 등록함수 `0x8003D2C4` 는 tag 교환(리스트 연결)만 함
- 음절을 다른 VRAM 페이지에 두려면 훅이 DR_TPAGE(GP0 0xE1) 프리미티브 삽입 필요

### ASCII 페이지 빈 영역 (tpage 없이 쓸 수 있는 공간)
- (896,0) 페이지에서 **V 144~255 (코드 0xB0~0xFF 영역) 완전히 비어있음**
- 약 110칸 → 최소 훅(tpage 없음)으로 음절 110개 배치 가능

### 훅 단계 설계
1. **최소 훅** (tpage 없음): 음절을 ASCII페이지 빈영역(V144~255)에.
   훅은 마커판별 + U/V재계산 + s3+=2 만. ~110음절.
2. **전체 훅** (tpage 포함): 다중 페이지로 ~900음절.

### VRAM 여유
`(832, 0)` 에 폰트 페이지 하나 추가 배치 가능 (검증됨)

---

## 9. 도구 (추가)

| 파일 | 역할 |
|------|------|
| **`build_us_kr3.py`** | **완성형 음절 PoC (성공, 품질 우수)** |
| `build_us_kr2.py` | 초기 한글 PoC (4자, 폭 패치 포함) |
| `write_us3.py` | KR3용 디스크 기록 |
| `jamo.py` / `jamo2.py` | 조합형 자모 (폐기, 참고용) |

---

## 8. 도구

| 파일 | 역할 |
|------|------|
| `disc.py` | Mode2/2352 + ISO9660 |
| `prot.py` / `legaia.py` | PROT→PACK→LZSS→Script |
| `lzss.py` / `lzss_fast.c` | LZSS (라운드트립 검증) |
| `diffsec.c` | 디스크 섹터 비교 |
| `hangul.py` | 한글 글리프 12×12 (Unifont) |
| **`build_us_kr2.py`** | **한글 PoC 빌더 (성공)** |
| **`write_us.py`** | **PROT+EXE 디스크 기록, EDC/ECC 재계산** |

```bash
pip install pillow numpy capstone fonttools --break-system-packages
gcc -O2 -o tools/lzss_fast tools/lzss_fast.c
```

---

## 10. TODO

1. **[다음] 훅 코드 배치 자리 확정** — EXE 확장 가능 여부를 에뮬레이터로 검증
   (0x8007B800 이후를 게임이 쓰는지 메모리 감시)
2. **2바이트 훅 구현** — MIPS 어셈블리, tpage 전환 포함
3. 음절 폰트 페이지 생성 (VRAM 832,0 또는 896,256)
4. 전체 텍스트 덤프 → 번역 → 재삽입 파이프라인
5. 메뉴/아이템/전투 UI (대사 외 영역, 별도 렌더러)
